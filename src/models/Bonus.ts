export interface Bonus {
    type: string,

    attackMod: Number;
    damageMod: Number;
    meleeAttack:  Number,

    damage1h: Number,
    damage2h: Number,
    extraAttacks: Number,

    fearSave: Number;
    charmSave: Number;
};